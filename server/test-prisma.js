const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const user = await prisma.user.findFirst(); const tx = await prisma.transaction.create({ data: { userId: user.id, type: 'expense', amount: 100, description: 'Test crash', date: new Date(), percentageRuleId: undefined } }); console.log('Success!', tx); } main().catch(console.error);
